import React, { Component } from 'react';
const Input = (title) => {
    return (
        <div className="input">
            <input></input>
            <p>{title}</p>
        </div>
    )
}
export default Input;